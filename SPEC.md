# Slay the Text 项目规格

## 项目定位

Slay the Text 是一个基于 Python 的《杀戮尖塔》文字版命令行游戏，支持 Silent、Ironclad 和 Defect 三名角色。

## 架构

- `main.py`：程序入口、主循环和异常日志。
- `entities.py`：角色、卡牌、药水、遗物和事件数据，以及遭遇战流程。
- `climber.py`：角色状态、抽牌、出牌、战斗和牌组操作。
- `enemy.py`：敌人状态、意图、行动和伤害结算。
- `helping_functions.py`：地图后的奖励、商店和通用交互。
- `acts.py`：地图生成、节点连接和地图显示。
- `save_handlery.py`：存档读取与保存。
- `i18n.py`：中文显示层和中文名称输入适配；不修改游戏内部英文键值。
- `ai_director.py`：AI 事件主持人、每回合战斗主持、自然语言动作转换、离线事件和记忆记录。
- `ai_service.py`：OpenAI Responses/兼容接口适配；在 pi 环境中自动读取当前 `PI_PROVIDER`、`PI_MODEL` 和 `~/.pi/agent/models.json`，显式 `.env` 配置优先。
- `ai_schema.py`：AI 返回内容和效果白名单校验。
- `ai_memory.py`：保存最近的事件选择，供后续 AI 事件参考。
- `ai_parser.py`：自然语言战斗指令和敌人目标解析，并校验 AI 转换后的动作只引用当前手牌/药水。

## 本地化策略

1. 游戏逻辑继续使用英文名称作为稳定的内部 ID，避免破坏条件判断和已有存档。
2. 输出通过统一的 `ansiprint` 和内置 `print` 适配器翻译，覆盖菜单、状态、奖励、卡牌、遗物、药水、敌人和常用战斗术语。
3. 输入通过内置 `input` 适配器转换中文卡牌、遗物和药水名称；数字选项和英文名称仍然有效。
4. 战斗每回合开始时，AI DM 读取当前战斗快照，用自然语言叙述玩家生命/能量、敌人血量和即将执行的动作；不再依赖固定的“威胁/提示”栏目，AI 不执行动作、不直接修改状态。
5. 战斗界面使用固定面板统一展示状态、敌人、手牌、药水和操作；出牌与药水产生的内部过程消息先收集，再由 AI 根据前后状态流式整理为一段行动结果，在线请求失败时使用本地摘要。
6. 战斗输入支持数字和自然语言；自然语言优先交给 AI 转换为单个白名单动作，再调用原有游戏函数执行；无在线模型时才使用本地解析兜底。
7. AI 事件只返回白名单效果，所有数值和卡牌/遗物名称经过 `ai_schema.py` 校验。
8. ANSI 颜色标记保留，中文文本继续使用原有的彩色终端输出。

## 启动与验证

```bash
python -m pip install ansimarkup colorama
python main.py
python -m compileall -q .
```

## 任务清单

- [x] 拉取并整理上游源码。
- [x] 安装 Python 运行依赖。
- [x] 增加统一中文显示层。
- [x] 接入主程序、地图、角色、敌人、事件和奖励模块。
- [x] 增加中文名称查询兼容。
- [x] 增加离线 AI 事件主持人和可选在线模型接口。
- [x] 增加 AI 效果白名单和事件记忆。
- [x] 增加战斗中的自然语言指令和目标选择。
- [x] 接入每个战斗回合的 AI 主持旁白和安全动作转换。
- [x] 通过 Act 1 至 Act 4 的快速全流程通关测试，并修复长回合意图、首领转场和终局结算问题。
- [x] 在线 DM 使用 SSE 流式输出；战斗自然语言动作优先交给模型判断并解析模型返回的 JSON 意图。
- [x] 完成编译与启动流程验证。
- [ ] 根据实际游玩反馈继续润色事件叙事文本。
